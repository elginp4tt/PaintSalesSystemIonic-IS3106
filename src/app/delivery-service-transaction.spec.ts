import { DeliveryServiceTransaction } from './delivery-service-transaction';

describe('DeliveryServiceTransaction', () => {
  it('should create an instance', () => {
    expect(new DeliveryServiceTransaction()).toBeTruthy();
  });
});
