import { MixedPaintTransaction } from './mixed-paint-transaction';

describe('MixedPaintTransaction', () => {
  it('should create an instance', () => {
    expect(new MixedPaintTransaction()).toBeTruthy();
  });
});
