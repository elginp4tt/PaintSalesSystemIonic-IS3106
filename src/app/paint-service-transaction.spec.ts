import { PaintServiceTransaction } from './paint-service-transaction';

describe('PaintServiceTransaction', () => {
  it('should create an instance', () => {
    expect(new PaintServiceTransaction()).toBeTruthy();
  });
});
